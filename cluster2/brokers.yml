apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: brokers2
  namespace: kafka
  labels:
    strimzi.io/cluster: ceps2
spec:
  replicas: 3
  roles:
    - broker
  storage:
    type: jbod
    volumes:
      - id: 0
        type: persistent-claim
        size: 50Gi
        deleteClaim: false
